---
phase: 05-shopping-list-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [supabase/migrations/20260415000000_shopping_list.sql, supabase/functions/categorize-ingredients/index.ts, src/lib/services/planner.ts]
autonomous: true
requirements: [SHOP-01, SHOP-04]
user_setup: []
must_haves:
  truths:
    - "New shopping_list_items table exists in Supabase"
    - "Edge Function categorize-ingredients can process a list of ingredients into categories"
    - "Saving a meal plan triggers the categorization process"
  artifacts:
    - path: "supabase/migrations/20260415000000_shopping_list.sql"
      provides: "Database table and RLS policies"
    - path: "supabase/functions/categorize-ingredients/index.ts"
      provides: "AI-based categorization logic"
    - path: "src/lib/services/planner.ts"
      provides: "Trigger for categorization during meal plan save"
  key_links:
    - from: "src/lib/services/planner.ts"
      to: "supabase/functions/categorize-ingredients"
      via: "supabase.functions.invoke"
---

<objective>
Implement the backend infrastructure for the shopping list, including database storage and AI-powered categorization.

Purpose: To extract ingredients from meal plans and group them into logical grocery store aisles using AI.
Output: Migration for `shopping_list_items` table and the `categorize-ingredients` Supabase Edge Function.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-shopping-list-consolidation/05-RESEARCH.md
@.planning/phases/05-shopping-list-consolidation/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shopping_list_items table migration</name>
  <files>supabase/migrations/20260415000000_shopping_list.sql</files>
  <action>
    Create a new migration file:
    - Table `public.shopping_list_items`:
      - `id` UUID PRIMARY KEY DEFAULT gen_random_uuid()
      - `meal_plan_id` UUID NOT NULL REFERENCES public.meal_plans(id) ON DELETE CASCADE
      - `original_string` TEXT NOT NULL
      - `category` TEXT NOT NULL
      - `created_at` TIMESTAMPTZ NOT NULL DEFAULT now()
    - Enable RLS on the table.
    - Policy: "Users can manage shopping items for their own meal plans" (follow pattern from meal_plan_slots).
    - Add unique constraint on `(meal_plan_id, original_string)` to prevent duplicate categorized entries.
  </action>
  <verify>
    <automated>cat supabase/migrations/20260415000000_shopping_list.sql | grep "CREATE TABLE IF NOT EXISTS public.shopping_list_items"</automated>
  </verify>
  <done>Migration file created with correct table structure and RLS policies.</done>
</task>

<task type="auto">
  <name>Task 2: Implement categorize-ingredients Edge Function</name>
  <files>supabase/functions/categorize-ingredients/index.ts</files>
  <action>
    Create a new Edge Function that:
    1. Accepts `meal_plan_id` and a list of `ingredients` (strings).
    2. Deduplicates the input ingredient strings to save tokens.
    3. Calls the existing `ai-proxy` function with a prompt to categorize each ingredient.
    4. Categorization prompt MUST specify valid categories: [Produce, Meat & Seafood, Dairy & Eggs, Pantry & Grains, Canned & Jarred, Bakery, Frozen Foods, Condiments & Spices, Snacks & Sweets, Beverages, Deli, Household].
    5. AI should return JSON mapping ingredient to category.
    6. Upserts the categorized items into `public.shopping_list_items`.
    7. Use `createClient` with service role or auth header (if user is authenticated). For Edge Functions calling other Edge Functions, passing the Authorization header is standard.
  </action>
  <verify>
    <automated>ls supabase/functions/categorize-ingredients/index.ts</automated>
  </verify>
  <done>Edge Function implemented and ready for deployment.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate categorization trigger into plannerService</name>
  <files>src/lib/services/planner.ts</files>
  <action>
    Modify `saveMealPlan` in `src/lib/services/planner.ts` to trigger the `categorize-ingredients` Edge Function after successful save:
    1. Collect all ingredients from the saved recipes.
    2. Extract just the strings from the `ingredients` JSONB array (which are stored as simple strings per research).
    3. Call `supabase.functions.invoke('categorize-ingredients', { body: { meal_plan_id: plan.id, ingredients } })`.
    4. Do not wait for the response (non-blocking) or handle errors gracefully (it's a secondary process).
  </action>
  <verify>
    <automated>grep "supabase.functions.invoke('categorize-ingredients'" src/lib/services/planner.ts</automated>
  </verify>
  <done>Trigger logic added to `saveMealPlan` method.</done>
</task>

</tasks>

<verification>
Check for migration file, edge function code, and service integration.
</verification>

<success_criteria>
- Backend infrastructure for shopping lists exists.
- Categorization logic is ready and triggered when a meal plan is saved.
</success_criteria>

<output>
After completion, create `.planning/phases/05-shopping-list-consolidation/05-01-SUMMARY.md`
</output>
