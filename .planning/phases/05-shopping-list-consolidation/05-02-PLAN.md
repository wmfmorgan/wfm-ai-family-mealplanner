---
phase: 05-shopping-list-consolidation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [src/pages/MealPlanner/ShoppingList.tsx, src/pages/MealPlanner/ShoppingList.css, src/components/Layout/BottomTabBar.tsx, src/App.tsx]
autonomous: false
requirements: [SHOP-01, SHOP-02, SHOP-03]
user_setup: []
must_haves:
  truths:
    - "New Shopping tab exists in the BottomTabBar"
    - "Shopping list screen displays ingredients grouped by category"
    - "Checklist state persists via localStorage for the duration of the visit"
    - "Shopping list looks correct when printed"
  artifacts:
    - path: "src/pages/MealPlanner/ShoppingList.tsx"
      provides: "Main shopping list page with category grouping"
    - path: "src/pages/MealPlanner/ShoppingList.css"
      provides: "Styling and Print-specific CSS"
  key_links:
    - from: "src/pages/MealPlanner/ShoppingList.tsx"
      to: "supabase.from('shopping_list_items')"
      via: "fetch within useEffect"
---

<objective>
Implement the frontend for the shopping list, including the UI tab, category-based grouping, interactive checklist, and print optimization.

Purpose: To provide users with a functional, easy-to-use grocery list derived from their weekly meal plan.
Output: Shopping List page, Tab navigation, Checklist logic, and Print CSS.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-shopping-list-consolidation/05-RESEARCH.md
@.planning/phases/05-shopping-list-consolidation/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Shopping List UI via Google Stitch</name>
  <files>src/pages/MealPlanner/ShoppingList.tsx, src/pages/MealPlanner/ShoppingList.css</files>
  <action>
    Use Google Stitch pattern "The Market Ledger (Desktop)" (ID: 976efb805a9342938edac3b776bc32f0) to create the base components:
    1. Create `ShoppingList.tsx` and `ShoppingList.css`.
    2. Component structure:
       - Header: "Shopping List - Week of [Date]"
       - Content: Iterate over standard categories.
       - Category Section: Header with category name + List of items.
       - Item: Checkbox + Ingredient string.
    3. Use Vanilla CSS with earthy/cookbook tones.
    4. Implement grouping logic: Fetch `shopping_list_items` for the current week's `meal_plan_id`, group them by `category` in JavaScript before rendering.
  </action>
  <verify>
    <automated>ls src/pages/MealPlanner/ShoppingList.tsx</automated>
  </verify>
  <done>Shopping List page created with category-based grouping.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Checklist Logic and Print CSS</name>
  <files>src/pages/MealPlanner/ShoppingList.tsx, src/pages/MealPlanner/ShoppingList.css</files>
  <action>
    Add interaction and print-specific features:
    1. Interactive Checklist:
       - Use `useState` to track checked items.
       - Persist to `localStorage` using a key like `checked_items_[meal_plan_id]`.
       - Allow tapping/clicking the whole row to toggle check state.
    2. Print Optimization:
       - Add `@media print` rules to `ShoppingList.css`.
       - Hide navigation bars and buttons during print.
       - Use `column-count: 2` for the list to save space on paper.
       - Ensure category headers don't break across columns (`break-inside: avoid-column`).
    3. Add a "Print" button at the top of the list.
  </action>
  <verify>
    <automated>grep "@media print" src/pages/MealPlanner/ShoppingList.css</automated>
  </verify>
  <done>Checklist functionality and print styles implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Add Shopping Tab and Wire Navigation</name>
  <files>src/components/Layout/BottomTabBar.tsx, src/App.tsx</files>
  <action>
    Wire the new page into the application:
    1. Update `src/components/Layout/BottomTabBar.tsx` to include a "Shopping" tab (use Lucide `ShoppingCart` icon).
    2. Update `src/App.tsx` (or your router) to define the `/shopping` route pointing to `ShoppingList.tsx`.
    3. Ensure the current household and week context are passed or correctly retrieved in `ShoppingList.tsx`.
  </action>
  <verify>
    <automated>grep "Shopping" src/components/Layout/BottomTabBar.tsx</automated>
  </verify>
  <done>Shopping list accessible via the bottom navigation bar.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Full shopping list feature including backend categorization and frontend UI.</what-built>
  <how-to-verify>
    1. Generate or update a meal plan for the current week.
    2. Navigate to the new "Shopping" tab.
    3. Verify ingredients are correctly categorized (e.g., "Milk" in Dairy, "Carrots" in Produce).
    4. Verify items stay checked when navigating away and back (within same session).
    5. Click "Print" and verify the layout looks good in the print preview.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Check for existence of ShoppingList page, correct navigation, and print CSS.
</verification>

<success_criteria>
- Users can view a consolidated, categorized shopping list for their meal plan.
- Interactive checklist works for tracking shopping progress.
- Professional print output for the "Cookbook" experience.
</success_criteria>

<output>
After completion, create `.planning/phases/05-shopping-list-consolidation/05-02-SUMMARY.md`
</output>
