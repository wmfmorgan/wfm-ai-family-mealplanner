---
phase: 02-household-profiles
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [
  src/pages/Household/Household.tsx,
  src/pages/Household/Household.css,
  src/components/Household/MemberCard.tsx,
  src/components/Household/MemberCard.css,
  src/components/Household/MemberGrid.tsx,
  src/components/Household/MemberGrid.css,
  src/components/Household/ProfileForm.tsx,
  src/components/Household/ProfileForm.css,
  src/lib/services/household.ts
]
autonomous: false
requirements: [HH-03, HH-04, HH-05]
must_haves:
  truths:
    - "Users can see a list of household members"
    - "Users can edit nutrition profiles with presets"
    - "Users can toggle 'Top 9' allergens and add custom 'Chef's Notes'"
    - "Changes are persisted to Supabase and validated by the schema"
  artifacts:
    - path: "src/pages/Household/Household.tsx"
      provides: "The main household management view"
    - path: "src/components/Household/ProfileForm.tsx"
      provides: "The core form with presets and hybrid inputs"
    - path: "src/lib/services/household.ts"
      provides: "CRUD operations for household data"
  key_links:
    - from: "src/pages/Household/Household.tsx"
      to: "supabase.household_members"
      via: "src/lib/services/household.ts"
---

<objective>
Build the Household Management page using Google Stitch, featuring presets and a hybrid allergy system.

Purpose: Provide a delightful, tactile experience for managing family nutrition needs.
Output: A set of React components and a service layer for household management.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-household-profiles/02-CONTEXT.md
@.planning/phases/02-household-profiles/02-RESEARCH.md
@.planning/phases/02-household-profiles/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Household Service and Components</name>
  <files>
    src/lib/services/household.ts,
    src/components/Household/MemberCard.tsx,
    src/components/Household/MemberCard.css,
    src/components/Household/MemberGrid.tsx,
    src/components/Household/MemberGrid.css,
    src/components/Household/ProfileForm.tsx,
    src/components/Household/ProfileForm.css
  </files>
  <action>
    1.  **Service Layer**: Create `src/lib/services/household.ts` to handle fetching members, updating profiles, and adding/removing members. Use `supabase-js`.
    2.  **MemberCard**: Create a tactile card component (per D-03/Cookbook vibe) showing member name and summary icons (allergies, calorie target). Use earthy tones and generous padding.
    3.  **MemberGrid**: Create a responsive grid for `MemberCard` components.
    4.  **ProfileForm**: Create a comprehensive form with:
        - **Presets**: Row of tactile buttons for "Active Adult", "Growing Toddler", "Elderly", etc.
        - **Advanced Toggle**: To show/hide manual macro/calorie editing.
        - **Hybrid Allergy System**: Visual toggles for the "Top 9" (Peanuts, Tree Nuts, Milk, Egg, Wheat, Soy, Fish, Shellfish, Sesame) + a "Chef's Notes" textarea for custom avoidances.
        - **Appliances**: Checkboxes for the method-centric list (Slow Cooker, Air Fryer, Oven, Stove, Pressure Cooker).
    5.  **Styling**: Use Vanilla CSS only. Follow the "minimalist earthy aesthetic" and "cookbook layout" mandates. Use serif fonts for headings and sans-serif for body text.
  </action>
  <verify>
    <automated>
      ls src/lib/services/household.ts src/components/Household/MemberCard.tsx src/components/Household/ProfileForm.tsx
    </automated>
  </verify>
  <done>Service layer and core UI components implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Household Page and Wiring</name>
  <files>src/pages/Household/Household.tsx, src/pages/Household/Household.css, src/App.tsx</files>
  <action>
    1.  **Household Page**: Assemble the `MemberGrid` and `ProfileForm` into a cohesive page. Handle loading states and errors gracefully.
    2.  **State Management**: Use `React.useState` and `useEffect` to manage the list of members and the currently selected member for editing.
    3.  **Routing**: Update `src/App.tsx` (or the router config) to include the new Household page.
    4.  **Visual Audit**: Ensure the page follows the "minimalist earthy aesthetic" (soft greens, warm neutrals).
  </action>
  <verify>
    <automated>
      grep -q "Household" src/App.tsx && ls src/pages/Household/Household.tsx
    </automated>
  </verify>
  <done>Household page integrated and wired to the router.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Household management UI with profile editing.</what-built>
  <how-to-verify>
    1.  Navigate to /household.
    2.  Verify the "Me" member is present (auto-initialized).
    3.  Open the ProfileForm for "Me".
    4.  Select a Preset (e.g., "Active Adult") and verify values update.
    5.  Toggle some Allergies and add "No cilantro" in Chef's Notes.
    6.  Save and verify data persists (refresh page).
    7.  Check the visual aesthetic (earthy, cookbook-vibe, legible serif headings).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Complete a manual walkthrough of the household management flow, ensuring all constraints (presets, hybrid allergies, appliances) are met and persistence works correctly.
</verification>

<success_criteria>
- Household management UI is functional and follows the visual mandates (HH-03, HH-04, HH-05).
- All profile fields save correctly to the database.
- The UX is seamless and non-clinical.
</success_criteria>

<output>
After completion, create `.planning/phases/02-household-profiles/02-02-SUMMARY.md`
</output>
