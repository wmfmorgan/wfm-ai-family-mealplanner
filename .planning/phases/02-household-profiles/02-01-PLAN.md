# Plan: 02-01 - Database & Schema Refinements

## Goal
Implement the automatic household creation trigger and refine the `household_members` schema for nutrition profiles.

## Tasks
- [ ] Create a new Supabase migration for the `handle_new_user()` function and `on_auth_user_created` trigger.
- [ ] Add `is_owner` boolean to `public.household_members` to track the primary household contact.
- [ ] Update RLS policies if needed to ensure data security.
- [ ] Implement basic JSONB schema validation (Check constraint) on `nutrition_profile`.

## Verification
- [ ] Sign up as a new user (mocked or real) and verify a row in `public.households` is created automatically.
- [ ] Verify that a default household member is NOT created yet (or decide to create one in the trigger).
- [ ] Test JSONB validation with invalid data and ensure it fails.
