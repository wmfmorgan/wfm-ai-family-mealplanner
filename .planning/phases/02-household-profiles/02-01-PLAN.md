---
phase: 02-household-profiles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [supabase/migrations/20260412000000_household_refinements.sql]
autonomous: true
requirements: [HH-01, HH-02, HH-06]
must_haves:
  truths:
    - "New users automatically have a household created upon signup"
    - "New users automatically have a 'Me' member created in their household"
    - "Nutrition profiles are strictly validated against the required schema"
    - "Only household owners can manage their members"
  artifacts:
    - path: "supabase/migrations/20260412000000_household_refinements.sql"
      provides: "Schema updates, triggers, and validation functions"
  key_links:
    - from: "auth.users"
      to: "public.households"
      via: "on_auth_user_created trigger"
    - from: "public.households"
      to: "public.household_members"
      via: "handle_new_user function"
---

<objective>
Implement automatic household/member initialization and refine the schema for validated nutrition profiles.

Purpose: Ensure every user has a pre-initialized environment and that data integrity is maintained for the AI generation layer.
Output: Supabase migration file with triggers, functions, and schema refinements.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-household-profiles/02-CONTEXT.md
@.planning/phases/02-household-profiles/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Migration for Auto-Initialization and Schema Refinements</name>
  <files>supabase/migrations/20260412000000_household_refinements.sql</files>
  <action>
    Create a new migration file that includes:
    1.  A function `handle_new_user()` that:
        - Inserts a new row into `public.households` using the `new.id` from `auth.users` as `owner_id`.
        - Inserts a new row into `public.household_members` linked to the new household, with the name 'Me', `is_owner = true`, and a default `nutrition_profile`.
    2.  A trigger `on_auth_user_created` on `auth.users` that calls `handle_new_user()` after an insert.
    3.  Schema refinements for `public.household_members`:
        - Add `is_owner` (BOOLEAN, DEFAULT false).
        - Add `is_active` (BOOLEAN, DEFAULT true).
    4.  A validation function `validate_nutrition_profile(profile JSONB)` that checks for:
        - `target_calories` (number)
        - `macro_targets` (object with `protein_pct`, `carbs_pct`, `fat_pct` numbers)
        - `allergies` (array of strings)
        - `avoidances` (array of strings)
        - `appliances` (array of strings)
        - `cooking_skill` (string)
        - `is_child` (boolean)
    5.  A CHECK constraint on `public.household_members` using the validation function.
    6.  Ensure RLS policies are updated to reflect these changes (HH-06).
  </action>
  <verify>
    <automated>
      # This requires running the migration against a local Supabase instance if available, 
      # but for this task, we will verify the SQL syntax and structure.
      grep -q "CREATE FUNCTION public.handle_new_user" supabase/migrations/20260412000000_household_refinements.sql && \
      grep -q "CREATE TRIGGER on_auth_user_created" supabase/migrations/20260412000000_household_refinements.sql && \
      grep -q "CHECK (validate_nutrition_profile(nutrition_profile))" supabase/migrations/20260412000000_household_refinements.sql
    </automated>
  </verify>
  <done>Migration file created with all required components.</done>
</task>

</tasks>

<verification>
Verify the migration script by checking for the presence of the `handle_new_user` function, the `on_auth_user_created` trigger, and the `validate_nutrition_profile` check constraint.
</verification>

<success_criteria>
- The migration file correctly implements the auto-initialization logic (HH-01).
- The schema is refined with ownership and activity flags, and JSONB validation (HH-02).
- RLS policies are correctly configured (HH-06).
</success_criteria>

<output>
After completion, create `.planning/phases/02-household-profiles/02-01-SUMMARY.md`
</output>
