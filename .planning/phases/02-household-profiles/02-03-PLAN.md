# Plan: Phase 2.3 - Refinement & Fixes

## Goal
Address gaps identified during UAT, including missing deletion functionality, macro validation, and visibility of Chef's Notes.

## Tasks

### 1. Delete Functionality
- [x] **Household Service**: Ensure `deleteMember` is robust (already exists, but verify it doesn't delete the owner/last member without warning).
- [x] **MemberCard**: Add a delete button (icon or text) with appropriate styling.
- [x] **Household Page**: Implement `handleDeleteMember` with a `window.confirm` dialog.

### 2. Macro Validation & Guardrails
- [x] **ProfileForm**: Add a derived state to calculate total macro percentage.
- [x] **UI Feedback**: Display a warning message if macros != 100% in Manual Mode.
- [x] **Save Guard**: Disable "Save Changes" button if macros != 100% in Manual Mode.

### 3. Visibility & Input Improvements
- [x] **ProfileForm**: Fix the "one-word" bug in Chef's Notes by allowing spaces during typing (debouncing or local state).
- [x] **MemberGrid**: Pass `avoidances` (Chef's Notes) to `MemberCard`.
- [x] **MemberCard**: Display `avoidances` as secondary tags (distinct style from allergies).
- [x] **Styling**: Ensure tags don't clutter the card (maybe limit to first 3 or use "..." if too many).

## Verification Criteria
- [x] Delete a non-owner member and verify it's removed from UI and DB/MockState.
- [x] Try to save a profile with 110% macros and verify "Save" is disabled/warned.
- [x] Add "No cilantro" to notes and verify it appears on the MemberCard.
