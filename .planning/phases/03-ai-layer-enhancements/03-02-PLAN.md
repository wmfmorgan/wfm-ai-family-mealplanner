---
phase: 03-ai-layer-enhancements
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - "supabase/functions/ai-proxy/index.ts"
  - "src/lib/ai/client.ts"
autonomous: true
requirements:
  - "AI-01"
user_setup:
  - service: gemini
    why: "AI generation provider integration"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio"
  - service: grok
    why: "AI generation provider integration"
    env_vars:
      - name: XAI_API_KEY
        source: "xAI Console"
must_haves:
  truths:
    - "Active AI model responds to requests"
    - "AI errors are immediately visible"
    - "Developer can trace request latency and payloads in local logs"
  artifacts:
    - path: "supabase/functions/ai-proxy/index.ts"
      provides: "Provider routing and OpenAI compatibility layer"
    - path: "src/lib/ai/client.ts"
      provides: "Frontend fetch wrapper to AI proxy"
  key_links:
    - from: "src/lib/ai/client.ts"
      to: "supabase/functions/ai-proxy/index.ts"
      via: "supabase.functions.invoke"
    - from: "src/lib/ai/client.ts"
      to: "src/lib/ai/logger.ts"
      via: "saveAiLog()"
---

<objective>
Update `ai-proxy` to support multi-provider routing (Gemini, Grok) using standard OpenAI REST format, and create the frontend `client.ts` wrapper that intercepts calls and invokes the logger.

Purpose: Establishes the foundation for provider switching and AI response debugging.
Output: Functional Edge Function supporting multiple models, and frontend client wrapper.
</objective>

<context>
@.planning/phases/03-ai-layer-enhancements/03-CONTEXT.md
@.planning/phases/03-ai-layer-enhancements/03-RESEARCH.md
</context>

<interfaces>
From src/lib/ai/logger.ts:
```typescript
export interface AILog {
  id: string;
  timestamp: string;
  provider: string;
  prompt: string;
  response?: string;
  latency: number;
  status_code: number;
  error?: string;
}
export function saveAiLog(log: Omit<AILog, 'id' | 'timestamp'>): void;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update AI Proxy Edge Function</name>
  <files>supabase/functions/ai-proxy/index.ts</files>
  <action>
    Update the edge function to accept `{ provider, prompt, ping, model }` in the JSON payload.
    If `ping=true`, return status 'ok' (keep existing logic).
    For AI generation, check the `provider`:
    - If `grok`: fetch `https://api.x.ai/v1/chat/completions`, use `XAI_API_KEY` from Deno.env, default model `grok-beta`.
    - If `gemini`: fetch `https://generativelanguage.googleapis.com/v1beta/openai/chat/completions`, use `GEMINI_API_KEY` from Deno.env, default model `gemini-2.5-flash`.
    Use the standard OpenAI POST payload structure for both: `{ model: model || defaultModel, messages: [{ role: 'user', content: prompt }] }`.
    Return the raw JSON response or error. Ensure `corsHeaders` are included in all responses, especially in the `catch` block (Fail-Fast policy D-02).
  </action>
  <verify>
    <automated>deno check supabase/functions/ai-proxy/index.ts</automated>
  </verify>
  <done>Proxy handles both providers using the OpenAI schema format and passes errors without retry.</done>
</task>

<task type="auto">
  <name>Task 2: Create AI Client Wrapper</name>
  <files>src/lib/ai/client.ts</files>
  <action>
    Create a wrapper function `askAI({ prompt, provider, model })` that:
    1. Records the start time (`performance.now()`).
    2. Calls the Supabase Edge Function `ai-proxy` using `supabase.functions.invoke`.
    3. Calculates latency.
    4. Calls `saveAiLog` (imported from `logger.ts`) with the request, response payload, latency, and HTTP status code. If an error occurs, save it in the error field of the log.
    5. Returns the successful response or throws the error.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Client wrapper successfully invokes the proxy, measures latency, and logs the interaction to localStorage.</done>
</task>

</tasks>

<verification>
TypeScript compilation and Deno checks pass for both frontend client and edge function.
</verification>

<success_criteria>
Proxy routes correctly to Grok and Gemini. Client wrapper intercepts and logs metrics.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-layer-enhancements/03-02-SUMMARY.md`
</output>