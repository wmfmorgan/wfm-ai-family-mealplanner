---
phase: 03-ai-layer-enhancements
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - "src/lib/ai/logger.ts"
  - "src/__tests__/ai-logger.test.ts"
autonomous: true
requirements:
  - "AI-02"
must_haves:
  truths:
    - "Developer can view recent AI prompts and responses"
    - "Developer observes that no more than 10 logs are retained in local storage"
    - "Developer retrieves logs ordered with the most recent first"
  artifacts:
    - path: "src/lib/ai/logger.ts"
      provides: "Logger functions and types"
      exports: ["saveAiLog", "getAiLogs", "AILog"]
    - path: "src/__tests__/ai-logger.test.ts"
      provides: "Unit tests for logger"
  key_links: []
---

<objective>
Implement the frontend AI logger utility using Test-Driven Development.

Purpose: Meets D-04 constraint for transient, db-free logging without polluting the database.
Output: Tested logger module using localStorage ready for integration.
</objective>

<tasks>
<task type="tdd">
  <name>AI Logger Utility</name>
  <files>src/lib/ai/logger.ts, src/__tests__/ai-logger.test.ts</files>
  <behavior>
    - Test 1: Saving a log adds it to localStorage under a specific key (`ai_logs`).
    - Test 2: Saving an 11th log evicts the oldest log (max 10 constraint).
    - Test 3: `getAiLogs()` returns logs sorted by newest first.
  </behavior>
  <implementation>
    Define `AILog` interface: `{ id: string, timestamp: string, provider: string, prompt: string, response?: string, latency: number, status_code: number, error?: string }`.
    Implement `saveAiLog(log: Omit<AILog, 'id' | 'timestamp'>)` that generates a unique ID and ISO timestamp, retrieves existing logs from localStorage, prepends the new log, slices the array to a maximum of 10 items, and saves it back to localStorage.
    Implement `getAiLogs(): AILog[]` that retrieves the logs and returns an empty array if none exist.
  </implementation>
  <verify>
    <automated>npm run test -- run src/__tests__/ai-logger.test.ts</automated>
  </verify>
  <done>Logger correctly handles localStorage operations and enforces the max 10 logs constraint.</done>
</task>
</tasks>

<verification>
`npm run test -- run src/__tests__/ai-logger.test.ts` passes.
</verification>

<success_criteria>
Logger correctly handles localStorage operations and enforces the max 10 logs constraint.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-layer-enhancements/03-01-SUMMARY.md`
</output>