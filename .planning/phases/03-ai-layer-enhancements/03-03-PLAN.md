---
phase: 03-ai-layer-enhancements
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - "src/pages/Settings/Settings.tsx"
  - "src/pages/Settings/Settings.css"
  - "src/pages/Settings/DebugLog.tsx"
  - "src/App.tsx"
autonomous: false
requirements:
  - "AI-03"
must_haves:
  truths:
    - "User can view a Developer Settings section on the Settings page"
    - "User can switch the active AI provider (Gemini/Grok)"
    - "User can view the 10 most recent AI interactions (Debug UI)"
  artifacts:
    - path: "src/pages/Settings/Settings.tsx"
      provides: "Main settings view and provider toggle"
    - path: "src/pages/Settings/DebugLog.tsx"
      provides: "Visual display of recent localStorage AI logs"
  key_links:
    - from: "src/pages/Settings/Settings.tsx"
      to: "src/pages/Settings/DebugLog.tsx"
      via: "React render"
    - from: "src/pages/Settings/DebugLog.tsx"
      to: "src/lib/ai/logger.ts"
      via: "getAiLogs()"
---

<objective>
Implement the Settings and Debug UI, replacing the placeholder in `App.tsx`.

Purpose: Fulfills constraints D-05 and D-06 to provide a transient developer debugging UI for inspecting AI payloads and prompt latency before moving to Phase 4.
Output: Fully functioning Settings page with provider toggle and Debug Log viewer.
</objective>

<context>
@.planning/phases/03-ai-layer-enhancements/03-CONTEXT.md
@.planning/phases/03-ai-layer-enhancements/03-RESEARCH.md
</context>

<interfaces>
From src/lib/ai/logger.ts:
```typescript
export interface AILog {
  id: string;
  timestamp: string;
  provider: string;
  prompt: string;
  response?: string;
  latency: number;
  status_code: number;
  error?: string;
}
export function getAiLogs(): AILog[];
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settings and Debug Log Components</name>
  <files>src/pages/Settings/Settings.tsx, src/pages/Settings/Settings.css, src/pages/Settings/DebugLog.tsx, src/__tests__/settings.test.tsx</files>
  <behavior>
    - Test 1: Settings renders provider selection (Gemini/Grok) correctly.
    - Test 2: DebugLog maps over AILogs array and renders them gracefully.
  </behavior>
  <action>
    Implement `Settings.tsx` to include a "Developer Settings" section allowing selection between `gemini` and `grok` (stored in `localStorage` as `active_ai_provider`).
    Explicitly import and render the `<DebugLog />` component inside `Settings.tsx` within the Developer Settings section.
    Implement `DebugLog.tsx` using `lucide-react` icons and a simple `<pre>` block to display the JSON payloads, latency (ms), and status code from the `getAiLogs()` output. Use a refresh button to re-fetch logs from localStorage.
    Apply the minimalist "cookbook" aesthetic using vanilla CSS in `Settings.css`. Maintain earthy tones and simple typography.
  </action>
  <verify>
    <automated>npm run test -- run src/__tests__/settings.test.tsx</automated>
  </verify>
  <done>Settings and Debug UI function correctly and look consistent with the app's aesthetic.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Settings to App Shell</name>
  <files>src/App.tsx</files>
  <action>
    Remove the `const Settings = () => ...` placeholder in `src/App.tsx`.
    Import the newly created `Settings` page component (`src/pages/Settings/Settings.tsx`).
    Ensure the router handles `<Route path="/settings" element={<Settings />} />` correctly.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>App.tsx is wired properly without TypeScript errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint UI Verification</name>
  <action>Human verification of visual layout and toggle persistence.</action>
  <what-built>Settings page with Provider Toggle and AI Debug Log viewer.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and navigate to `/settings`.
    2. Confirm "Developer Settings" exists and successfully allows toggling the active provider.
    3. Ensure the provider choice is saved across page reloads.
    4. Confirm the "Debug Log" component renders and correctly reads recent AI interactions from local storage.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/functional issues.</resume-signal>
</task>

</tasks>

<verification>
Unit tests pass, TypeScript compilation succeeds, and manual visual verification is approved.
</verification>

<success_criteria>
Developer debugging UI replaces placeholder, logs display accurately, and active provider preference persists.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-layer-enhancements/03-03-SUMMARY.md`
</output>