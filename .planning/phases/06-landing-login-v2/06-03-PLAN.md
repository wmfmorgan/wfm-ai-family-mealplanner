---
phase: 06-landing-login-v2
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: [src/pages/Auth/Login.tsx, src/pages/Auth/Auth.css, src/__tests__/login.test.tsx]
autonomous: true
requirements: [AUTH-V2-01]
must_haves:
  truths:
    - "Landing/Login page uses a 60/40 split-hero layout on desktop"
    - "Feature grid with 2x2 masonry cards is visible below the fold"
    - "Branded footer with WFM AI logo and tagline is present"
    - "Typography uses Newsreader (Serif) and Manrope (Sans-Serif) with correct casing (Small-Caps)"
  artifacts:
    - path: "src/pages/Auth/Login.tsx"
      provides: "Updated structure for split-hero, feature grid, and footer"
    - path: "src/pages/Auth/Auth.css"
      provides: "New styles for v2 desktop layout, including masonry grid and hero section"
---

<objective>
Implement missing v2 design elements on the Landing & Login page to achieve high-fidelity parity with the Stitch design.

Purpose: Move from a functional shell to a polished, editorial "cookbook" landing experience.
Output: Refactored Login page with split-hero, feature grid, and branded footer.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-landing-login-v2/06-RESEARCH.md
@.planning/phases/06-landing-login-v2/06-CONTEXT.md
@src/pages/Auth/Login.tsx
@src/pages/Auth/Auth.css
@src/styles/theme.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure Login.tsx for Split-Hero & Feature Grid</name>
  <files>src/pages/Auth/Login.tsx</files>
  <action>
    - Wrap current content in a `landing-wrapper` container.
    - Create a `hero-section` with `hero-image-container` (60%) and `auth-form-container` (40%).
    - Add a placeholder `img` or `div` with a background for the hero image.
    - Overlay the serif quote on the hero image.
    - Implement the "Intelligent Meal Architecture" section below the hero.
    - Add a 2x2 grid of feature cards (AI Recipe Preservation, Household Sync, Smart Pantry, Joy of Gathering).
    - Add the "OR CONTINUE WITH" divider and social login buttons (iCloud, Google) - UI ONLY.
    - Add policy links below the form.
    - Add the branded footer at the very bottom.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Login.tsx has all the structural components of the v2 design.</done>
</task>

<task type="auto">
  <name>Task 2: Implement v2 Desktop Styling in Auth.css</name>
  <files>src/pages/Auth/Auth.css</files>
  <action>
    - Implement the 60/40 flex/grid layout for `hero-section`.
    - Style the hero image with warm tones and the quote overlay.
    - Create masonry-style layout for feature cards using CSS Grid or Flex.
    - Use `font-variant: small-caps` or `text-transform: uppercase` with letter-spacing for labels and buttons per design.
    - Style the feature cards with the specified backgrounds (Sage Green, etc.) and icons.
    - Style the social auth divider and buttons.
    - Style the branded footer (centered, small-caps tagline).
    - Ensure responsive behavior: stack split-hero and grid on mobile.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Auth.css provides the editorial "cookbook" aesthetic and correct layout.</done>
</task>

<task type="auto">
  <name>Task 3: Update Login Tests</name>
  <files>src/__tests__/login.test.tsx</files>
  <action>
    - Update tests to find elements in the new split-hero structure.
    - Verify that "Welcome Home" is now the primary header instead of "Cookbook".
    - Check for existence of feature grid section and footer text.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/login.test.tsx</automated>
  </verify>
  <done>Tests pass and reflect the new v2 UI structure.</done>
</task>

</tasks>

<verification>
Manual visual check and automated test suite pass.
</verification>

<success_criteria>
- [ ] Landing page matches the 60/40 split-hero layout on desktop.
- [ ] Feature grid with 4 distinct cards is visible below the fold.
- [ ] Footer contains "WFM AI" and the "REFINED PLANNING..." tagline.
- [ ] All functional auth (Magic Link, Password) still works within the new UI.
</success_criteria>

<output>
After completion, create `.planning/phases/06-landing-login-v2/06-03-SUMMARY.md`
</output>
