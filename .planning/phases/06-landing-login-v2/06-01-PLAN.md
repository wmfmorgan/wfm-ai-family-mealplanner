---
phase: 06-landing-login-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/styles/theme.css, src/index.css, src/pages/Auth/Login.tsx, src/pages/Auth/Auth.css]
autonomous: false
requirements: [AUTH-V2-01, AUTH-V2-03]
must_haves:
  truths:
    - "Login page background is Canvas Cream (#FDFCFB)"
    - "Headline typography uses Newsreader (Serif)"
    - "Layout is centered on desktop (1024px container)"
  artifacts:
    - path: "src/styles/theme.css"
      provides: "Global design tokens"
    - path: "src/pages/Auth/Login.tsx"
      provides: "Refactored v2 structure"
    - path: "src/pages/Auth/Auth.css"
      provides: "v2 cookbook aesthetics"
  key_links:
    - from: "src/pages/Auth/Login.tsx"
      to: "src/pages/Auth/Auth.css"
      via: "import"
---

<objective>
Update the global styling foundation and refactor the Login page to match the "Landing & Login (Desktop) [v2]" design from Google Stitch.

Purpose: Align the initial user entry point with the high-end editorial cookbook aesthetic.
Output: Modernized Login UI with Serif headings, Sage Green accents, and a centered desktop-first layout.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-landing-login-v2/06-RESEARCH.md
@.stitch/DESIGN.md
@src/styles/theme.css
@src/pages/Auth/Login.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync Global Theme & Global Styles</name>
  <files>src/styles/theme.css, src/index.css</files>
  <action>
    - Update `src/styles/theme.css` to ensure all v2 design tokens from `.stitch/DESIGN.md` are correctly defined as CSS variables.
    - Specifically check: `--color-canvas-cream` (#FDFCFB), `--color-sage-green` (#4A6741), and the font families for Newsreader (Serif) and Manrope (Sans-Serif).
    - Ensure `src/index.css` or `theme.css` sets the base background to `--color-canvas-cream` and default typography to `--font-body`.
  </action>
  <verify>
    <automated>grep -q "#FDFCFB" src/styles/theme.css && grep -q "Newsreader" src/styles/theme.css</automated>
  </verify>
  <done>Global variables match Stitch v2 Design System.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Login Component & Auth Styles</name>
  <files>src/pages/Auth/Login.tsx, src/pages/Auth/Auth.css</files>
  <action>
    - Refactor `src/pages/Auth/Login.tsx` JSX to implement the v2 structure (centered 1024px card).
    - Update `src/pages/Auth/Auth.css` with the new styles per RESEARCH.md:
      - Centered container with `min-height: 100vh` and Canvas Cream background.
      - Centered card (max-width: 1024px for container, likely 400-500px for actual form card within that or as specified in Stitch).
      - Editorial typography using `var(--font-display)` for "Cookbook" title.
      - Sage Green accents for primary buttons and focus states.
    - Ensure all existing auth logic (state, handlers, AuthContext usage) is preserved exactly (per AUTH-V2-02).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Login page structure and styling matches v2 Desktop design while preserving functionality.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Refactored Landing & Login v2 UI</what-built>
  <how-to-verify>
    1. Run `npm run dev`.
    2. Open browser to the login page (likely http://localhost:5173/login or root).
    3. Verify:
       - Background is Canvas Cream (#FDFCFB).
       - Layout is centered on desktop.
       - "Cookbook" title uses Newsreader Serif.
       - Buttons use Sage Green (#4A6741).
       - Typography matches "cookbook" editorial feel.
    4. Compare against Stitch Screen ID `c9f136a1fa5e460aa641c79e890535d7`.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Ensure the Login UI is visually consistent with the v2 design system and builds without errors.
</verification>

<success_criteria>
- [ ] Global CSS variables match Stitch DESIGN.md.
- [ ] Login page uses centered card layout.
- [ ] Typography and color palette align with v2 cookbook aesthetic.
- [ ] Build completes successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/06-landing-login-v2/06-01-SUMMARY.md`
</output>
