---
phase: 06-landing-login-v2
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [src/__tests__/auth.test.tsx, src/__tests__/login.test.tsx]
autonomous: true
requirements: [AUTH-V2-02]
must_haves:
  truths:
    - "Magic Link login sends email via Supabase"
    - "Password login allows entry to the app"
    - "Automated tests pass for login components"
  artifacts:
    - path: "src/__tests__/auth.test.tsx"
      provides: "Regression coverage for AuthContext"
    - path: "src/__tests__/login.test.tsx"
      provides: "Regression coverage for Login page"
  key_links:
    - from: "src/__tests__/login.test.tsx"
      to: "src/pages/Auth/Login.tsx"
      via: "testing-library render"
---

<objective>
Verify and test the authentication flow to ensure the v2 UI refactor did not break core functionality or introduce regressions.

Purpose: Guarantee stability and reliability of the Magic Link and Password login methods.
Output: Passing automated test suite and manual verification of the end-to-end auth flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-landing-login-v2/06-01-SUMMARY.md
@src/pages/Auth/Login.tsx
@src/__tests__/auth.test.tsx
@src/__tests__/login.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Functional Verification of Auth Flow</name>
  <files>src/pages/Auth/Login.tsx</files>
  <action>
    - Manually verify (via dev environment) that the Magic Link login still functions correctly:
      - Enter email.
      - Click "Send Magic Link".
      - Verify success message appears.
    - Manually verify that Password login (fallback) still functions correctly:
      - Toggle to "Password" mode.
      - Enter email and password.
      - Verify sign-in behavior.
    - Note: This is a manual-heavy task but can be supported by running the app.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Core authentication features (Magic Link + Password) remain functional after UI refactor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update Auth Regression Tests</name>
  <files>src/__tests__/auth.test.tsx, src/__tests__/login.test.tsx</files>
  <behavior>
    - Test: `src/__tests__/login.test.tsx` should render the "Cookbook" title with the correct role (heading).
    - Test: `src/__tests__/login.test.tsx` should find and interact with the email input and login buttons regardless of new UI structure.
    - Test: `src/__tests__/auth.test.tsx` should confirm that ProtectedRoute redirection still functions as expected.
  </behavior>
  <action>
    - Update the test files to align with the new v2 DOM structure created in Plan 01.
    - Ensure all tests pass using `vitest`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/auth.test.tsx src/__tests__/login.test.tsx</automated>
  </verify>
  <done>Automated tests are green and reflect the refactored v2 structure.</done>
</task>

</tasks>

<verification>
Run all auth-related tests and ensure they pass with the new UI.
</verification>

<success_criteria>
- [ ] Manual verification of Magic Link and Password login successful.
- [ ] `npm test` or specific vitest commands pass for auth and login.
- [ ] No regressions in ProtectedRoute logic.
</success_criteria>

<output>
After completion, create `.planning/phases/06-landing-login-v2/06-02-SUMMARY.md`
</output>
