---
phase: 04-meal-planner-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "supabase/migrations/20260413000000_meal_planner_core.sql"
  - "supabase/functions/ai-proxy/index.ts"
  - "src/lib/services/planner.ts"
  - "package.json"
autonomous: true
requirements: ["REC-01", "STATE-01", "GEN-01"]
must_haves:
  truths:
    - "Database schema includes tables for recipes, meal plans, and slots"
    - "AI proxy returns structured JSON and handles markdown fences"
    - "Planner service can fetch and save meal plans and recipes"
  artifacts:
    - path: "supabase/migrations/20260413000000_meal_planner_core.sql"
      provides: "Tables: recipes, meal_plans, meal_plan_slots"
    - path: "supabase/functions/ai-proxy/index.ts"
      provides: "JSON-mode support for AI calls"
    - path: "src/lib/services/planner.ts"
      provides: "Planner CRUD logic"
  key_links:
    - from: "src/lib/services/planner.ts"
      to: "public.meal_plans"
      via: "supabase.from('meal_plans')"
---

<objective>
Establish the database schema for meal plans and recipes, and update the AI proxy to support structured meal plan generation.

Purpose: Foundation for persistence and AI integration.
Output: Migrated database, updated Edge Function, and core planner service.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-meal-planner-core/04-RESEARCH.md
@.planning/phases/04-meal-planner-core/04-CONTEXT.md
@supabase/migrations/20260411000000_initial_schema.sql
@supabase/migrations/20260412000000_household_refinements.sql
@supabase/functions/ai-proxy/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database Schema Migration</name>
  <files>supabase/migrations/20260413000000_meal_planner_core.sql</files>
  <action>
    Create a new migration file with the following tables:
    - `recipes`: `id`, `household_id`, `name`, `ingredients` (jsonb), `instructions` (jsonb), `nutrition` (jsonb), `prep_time_min`, `cook_time_min`, `servings`, `created_at`.
    - `meal_plans`: `id`, `household_id`, `week_start_date` (date), `status` (default 'draft'), `created_at`.
    - `meal_plan_slots`: `id`, `meal_plan_id`, `day_of_week` (0-6), `meal_type` (breakfast, lunch, dinner, snack), `recipe_id`, `is_locked` (default false), `manual_entry` (text).
    Implement RLS policies for each table:
    - Users can only access data belonging to their own `household_id` (using `auth.uid()` through the `households` table).
    Ensure `week_start_date` is unique per `household_id`.
  </action>
  <verify>
    <automated>ls supabase/migrations/20260413000000_meal_planner_core.sql && grep -q "CREATE TABLE public.recipes" supabase/migrations/20260413000000_meal_planner_core.sql && grep -q "CREATE TABLE public.meal_plans" supabase/migrations/20260413000000_meal_planner_core.sql && grep -q "CREATE TABLE public.meal_plan_slots" supabase/migrations/20260413000000_meal_planner_core.sql</automated>
  </verify>
  <done>Tables and RLS policies created in Supabase.</done>
</task>

<task type="auto">
  <name>Task 2: AI Proxy Enhancement</name>
  <files>supabase/functions/ai-proxy/index.ts</files>
  <action>
    Update the `ai-proxy` Edge Function to:
    - Accept a `system_prompt` and `response_format` in the request body.
    - Pass `response_format: { type: 'json_object' }` to the provider (Gemini/Grok).
    - Implement a utility to strip markdown code blocks (e.g., ```json ... ```) from the AI response before returning, as some models include them even in JSON mode.
  </action>
  <verify>
    <automated>grep -q "response_format" supabase/functions/ai-proxy/index.ts && grep -q "replace(/.*json/g, '')" supabase/functions/ai-proxy/index.ts</automated>
  </verify>
  <done>AI proxy supports structured JSON and is resilient to markdown fences.</done>
</task>

<task type="auto">
  <name>Task 3: Planner Service & Setup</name>
  <files>src/lib/services/planner.ts, package.json</files>
  <action>
    - Install `date-fns` using `npm install date-fns`.
    - Create `src/lib/services/planner.ts` with the following functions:
      - `getMealPlan(householdId: string, weekStartDate: string)`: Fetches a plan and its slots.
      - `saveMealPlan(plan: MealPlan, recipes: Recipe[], slots: MealPlanSlot[])`: Saves everything in a single transaction (or sequential calls if transaction not available in frontend SDK - use `rpc` if needed for atomic saves).
      - `getRecipes(householdId: string)`: Fetches private recipes for the household.
  </action>
  <verify>
    <automated>ls src/lib/services/planner.ts && grep -q "export function getMealPlan" src/lib/services/planner.ts && grep -q "export function saveMealPlan" src/lib/services/planner.ts && grep -q "date-fns" package.json</automated>
  </verify>
  <done>Planner service implemented and date-fns installed.</done>
</task>

</tasks>

<verification>
Database migrations applied. Edge function updated and tested with ping. Planner service created.
</verification>

<success_criteria>
- Schema supports weekly meal plans with locked slots and private recipes.
- AI proxy returns clean JSON objects.
- Planner service handles basic CRUD operations.
</success_criteria>

<output>
After completion, create `.planning/phases/04-meal-planner-core/04-01-SUMMARY.md`
</output>
