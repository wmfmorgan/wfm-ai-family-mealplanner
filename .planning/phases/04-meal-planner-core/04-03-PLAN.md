---
phase: 04-meal-planner-core
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - "src/components/MealPlanner/MealSlot.tsx"
  - "src/pages/MealPlanner/MealPlanner.tsx"
  - "src/lib/services/planner.ts"
  - "src/components/MealPlanner/MealSlot.css"
autonomous: false
requirements: ["LOCK-01"]
must_haves:
  truths:
    - "User can lock a meal slot and see it visually distinct"
    - "User can manually edit meal slot text and have it saved"
    - "Regenerating a plan keeps locked slots unchanged"
  artifacts:
    - path: "src/components/MealPlanner/MealSlot.tsx"
      provides: "Locking and editing UI"
    - path: "src/components/MealPlanner/MealSlot.css"
      provides: "Tactile visual feedback for locking"
  key_links:
    - from: "src/components/MealPlanner/MealSlot.tsx"
      to: "src/lib/services/planner.ts"
      via: "updateSlot() call"
---

<objective>
Implement the "Lock & Edit" workflow and refine the "cookbook" aesthetic for the meal planner.

Purpose: Provides users with granular control over the generated plan.
Output: Interaction-heavy planner with locking and manual editing.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-meal-planner-core/04-02-SUMMARY.md
@.planning/phases/04-meal-planner-core/04-RESEARCH.md
@src/components/MealPlanner/MealSlot.tsx
@src/pages/MealPlanner/MealPlanner.tsx
</context>

<interfaces>
From src/lib/services/planner.ts (updated in 04-01):
```typescript
export function updateSlot(slotId: string, updates: Partial<MealPlanSlot>): Promise<void>;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Lock & Manual Edit Logic</name>
  <files>src/components/MealPlanner/MealSlot.tsx, src/components/MealPlanner/MealSlot.css, src/lib/services/planner.ts</files>
  <action>
    - Update `MealSlot` component to:
      - Add a "Lock/Unlock" toggle button (using a tactile icon, e.g., padlock).
      - Add an "Edit" mode where the meal name becomes an input field for manual overrides.
      - Reflect the "locked" state visually (e.g., subtle background change or border).
    - Implement `updateSlot` in `plannerService` to persist `is_locked` and `manual_entry` changes.
    - Style using **Vanilla CSS** to match the "tactile cookbook" vibe (D-06).
  </action>
  <verify>
    <automated>ls src/components/MealPlanner/MealSlot.css && npm test src/__tests__/calendar-ui.test.tsx</automated>
  </verify>
  <done>Locking and manual editing functionality is implemented and persists to the database.</done>
</task>

<task type="auto">
  <name>Task 2: Partial Regeneration Flow</name>
  <files>src/pages/MealPlanner/MealPlanner.tsx, src/lib/ai/prompts.ts</files>
  <action>
    - Update the generation logic in `MealPlanner`:
      - Identify slots that are "locked" by the user.
      - Pass these locked slots as "Constraints" in the AI prompt (system prompt).
      - Instruct the AI to build the rest of the plan around these constraints.
      - Upon receiving the AI response, only update the "unlocked" slots in the database.
  </action>
  <verify>
    <automated>npm test src/__tests__/meal-plan.test.ts</automated>
  </verify>
  <done>Regeneration respects user-locked meal slots.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint - Full Workflow Validation</name>
  <files>src/pages/MealPlanner/MealPlanner.tsx</files>
  <action>
    Validate the full Lock & Edit workflow:
    1. Generate a meal plan.
    2. "Lock" a specific meal (e.g., Tuesday Dinner).
    3. Manually edit another meal (e.g., Wednesday Lunch).
    4. Regenerate the plan.
    5. Verify that Tuesday Dinner remained unchanged and the manual edit was preserved.
  </action>
  <verify>
    <automated># Manual verification required. Run: npm run dev</automated>
  </verify>
  <done>Human verification of lock/edit workflow successful.</done>
</task>

<task type="auto">
  <name>Task 4: Visual Audit & UX Polish</name>
  <files>src/styles/theme.css, src/pages/MealPlanner/MealPlanner.tsx, src/components/MealPlanner/PlannerGrid.css</files>
  <action>
    - Perform a final visual audit against the "earthy cookbook" aesthetic.
    - Refine spacing, typography, and color palette (warm neutrals, soft greens).
    - Add subtle transitions for transitions between weeks or generation loading states.
    - Ensure accessibility (ARIA labels for icons/buttons).
  </action>
  <verify>
    <automated>grep -q "background-color" src/components/MealPlanner/MealSlot.css && grep -q "transition" src/styles/theme.css</automated>
  </verify>
  <done>Final visual polish completed.</done>
</task>

</tasks>

<verification>
Manual E2E testing of the lock/edit/regenerate flow. Final visual review of the UI.
</verification>

<success_criteria>
- User can lock meal slots they want to keep.
- AI respects locked slots during regeneration.
- User can manually override meal text.
- UI feels "tactile" and follows the earthy minimalist style.
</success_criteria>

<output>
After completion, create `.planning/phases/04-meal-planner-core/04-03-SUMMARY.md`
</output>
