---
phase: 04-meal-planner-core
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified: [src/pages/MealPlanner/MealPlanner.tsx, src/components/MealPlanner/RecipeDetail.tsx, src/components/MealPlanner/RecipeDetail.css]
autonomous: true
gap_closure: true
requirements: ["REC-01"]

must_haves:
  truths:
    - "Clicking a generated recipe shows details drawer."
    - "Recipe details drawer displays title, ingredients, and instructions."
    - "Clicking outside drawer or on close button hides it."
  artifacts:
    - path: "src/components/MealPlanner/RecipeDetail.tsx"
      provides: "Recipe details UI"
    - path: "src/components/MealPlanner/RecipeDetail.css"
      provides: "Cookbook aesthetic styling"
  key_links:
    - from: "src/pages/MealPlanner/MealPlanner.tsx"
      to: "src/components/MealPlanner/RecipeDetail.tsx"
      via: "rendering selectedRecipe state"
---

<objective>
Implement the Recipe Detail View identified as a gap in UAT (MP-UAT-06).

Purpose: Enable users to view ingredients and instructions for generated recipes.
Output: A functional "cookbook-style" side drawer for recipe details.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-meal-planner-core/04-UAT.md
@src/pages/MealPlanner/MealPlanner.tsx
@src/lib/services/planner.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture full recipe data in planner state</name>
  <files>src/pages/MealPlanner/MealPlanner.tsx</files>
  <action>
    - Update `loadPlan` to store the full `slot.recipe` object in `planData[dateStr][mealType].recipe`.
    - Update `handleGenerate` (mock branch) to also store the generated recipe object in the local `gridData` state.
  </action>
  <verify>
    <automated>npm test src/__tests__/meal-plan.test.ts</automated>
  </verify>
  <done>Plan data contains recipe details (ingredients/instructions) instead of just the name.</done>
</task>

<task type="auto">
  <name>Task 2: Create RecipeDetail drawer component</name>
  <files>src/components/MealPlanner/RecipeDetail.tsx, src/components/MealPlanner/RecipeDetail.css</files>
  <action>
    - Create `RecipeDetail` functional component.
    - Style it as a side drawer that slides in from the right.
    - Use serif typography for titles and "earthy cookbook" aesthetics (warm neutrals, soft greens).
    - Render `ingredients` as a list of strings and `instructions` as a numbered list.
    - Handle empty states and provide a close button.
  </action>
  <verify>
    <automated>npm test src/__tests__/calendar-ui.test.tsx</automated>
  </verify>
  <done>RecipeDetail component correctly renders recipe data in a stylish drawer.</done>
</task>

<task type="auto">
  <name>Task 3: Wire up RecipeDetail in MealPlanner</name>
  <files>src/pages/MealPlanner/MealPlanner.tsx</files>
  <action>
    - Add `selectedRecipe` state (type `Recipe | null`) to `MealPlanner`.
    - Update `onSlotClick` to set `selectedRecipe` from the `planData`.
    - Render `<RecipeDetail recipe={selectedRecipe} onClose={() => setSelectedRecipe(null)} />` at the end of the page.
  </action>
  <verify>
    <automated>npm test src/__tests__/app.test.ts</automated>
  </verify>
  <done>User can open and close the recipe detail drawer by clicking slots.</done>
</task>

</tasks>

<verification>
- Run `npm test` to ensure stability.
- Manual check: Generate a plan, click a meal slot with a recipe name, verify the drawer opens with full details.
- Verify closing works (button and overlay click).
</verification>

<success_criteria>
- MP-UAT-06 passes.
- Minimalist, earthy visual style is maintained in the drawer.
- Recipe detail view is accessible and clear.
</success_criteria>

<output>
After completion, create `.planning/phases/04-meal-planner-core/04-05-SUMMARY.md`
</output>
