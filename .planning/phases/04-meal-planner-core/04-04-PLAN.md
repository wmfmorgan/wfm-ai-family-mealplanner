---
phase: 04-meal-planner-core
plan: 04
type: execute
wave: 1
depends_on: [04-03]
files_modified: [src/pages/MealPlanner/MealPlanner.tsx, src/components/MealPlanner/PlannerGrid.tsx, src/components/MealPlanner/DayColumn.tsx]
autonomous: true
gap_closure: true
requirements: [LOCK-01, STATE-01]

must_haves:
  truths:
    - "Locked slots remain unchanged while others refresh."
    - "Locking/editing works even if slot.id is missing (mock mode/unsaved slots)."
    - "Date keys are consistent across grid and page components."
  artifacts:
    - path: "src/pages/MealPlanner/MealPlanner.tsx"
      provides: "Consistent date formatting and loosened guards"
    - path: "src/components/MealPlanner/PlannerGrid.tsx"
      provides: "Unified date string generation"
  key_links:
    - from: "src/components/MealPlanner/PlannerGrid.tsx"
      to: "src/pages/MealPlanner/MealPlanner.tsx"
      via: "onLockToggle/onEdit callbacks with date strings"
---

<objective>
Fix the slot locking mechanism and date consistency issues identified in UAT (MP-UAT-04).

Purpose: Ensure the user can reliably lock meal slots and that the UI state updates correctly across all components.
Output: Consistent date formatting and resilient event handlers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-meal-planner-core/04-UAT.md
@src/pages/MealPlanner/MealPlanner.tsx
@src/components/MealPlanner/PlannerGrid.tsx
@src/components/MealPlanner/DayColumn.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify date formatting across planner components</name>
  <files>src/components/MealPlanner/PlannerGrid.tsx, src/components/MealPlanner/DayColumn.tsx, src/pages/MealPlanner/MealPlanner.tsx</files>
  <action>
    - In `PlannerGrid.tsx`, replace `day.toISOString().split('T')[0]` with `format(day, 'yyyy-MM-dd')` from `date-fns`.
    - In `MealPlanner.tsx`, ensure `loadPlan` and `handleGenerate` use `addDays(weekStartDate, i)` and `format(..., 'yyyy-MM-dd')` instead of manual millisecond calculations for reliability.
    - Ensure `isToday` check in `DayColumn.tsx` uses the same unified format.
  </action>
  <verify>
    <automated>npm test src/__tests__/calendar-ui.test.tsx</automated>
  </verify>
  <done>Date keys are consistent (local time YYYY-MM-DD) across all planner components.</done>
</task>

<task type="auto">
  <name>Task 2: Loosen guards in handleLockToggle and handleEdit</name>
  <files>src/pages/MealPlanner/MealPlanner.tsx</files>
  <action>
    - Update `handleLockToggle` to perform the optimistic UI update (setPlanData) *before* or *independently* of the `plannerService.updateSlot` call.
    - Allow the operation to proceed even if `slot.id` is missing (just skip the service call but keep the UI update). This supports mock mode and unsaved slots.
    - Apply the same loosening to `handleEdit`.
  </action>
  <verify>
    <automated>npm test src/__tests__/meal-plan.test.ts</automated>
  </verify>
  <done>Locking and editing work visually even when no database ID is present.</done>
</task>

</tasks>

<verification>
- Run `npm test` to ensure no regressions in meal plan state management.
- Manual check: Open planner, click lock icon on a slot, verify it toggles visually.
- Click "Generate Plan" and verify locked slot persists.
</verification>

<success_criteria>
- MP-UAT-04 passes.
- No console errors related to undefined slots when clicking lock icons.
- Date mismatch between grid and page is resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/04-meal-planner-core/04-04-SUMMARY.md`
</output>
