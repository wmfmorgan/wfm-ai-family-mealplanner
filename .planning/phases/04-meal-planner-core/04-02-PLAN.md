---
phase: 04-meal-planner-core
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - "src/components/MealPlanner/PlannerGrid.tsx"
  - "src/components/MealPlanner/DayColumn.tsx"
  - "src/components/MealPlanner/MealSlot.tsx"
  - "src/components/MealPlanner/GenerationPanel.tsx"
  - "src/pages/MealPlanner/MealPlanner.tsx"
  - "src/App.tsx"
autonomous: false
requirements: ["PLAN-01", "GEN-01"]
must_haves:
  truths:
    - "Planner displays 7 columns starting with Sunday"
    - "User can navigate to next and previous weeks"
    - "Triggering generation results in a populated 7-day plan in the UI"
  artifacts:
    - path: "src/components/MealPlanner/PlannerGrid.tsx"
      provides: "Sunday-start weekly grid"
    - path: "src/pages/MealPlanner/MealPlanner.tsx"
      provides: "Main planner page logic"
    - path: "src/lib/ai/prompts.ts"
      provides: "Structured meal plan prompts"
  key_links:
    - from: "src/pages/MealPlanner/MealPlanner.tsx"
      to: "src/lib/ai/client.ts"
      via: "askAI() call"
    - from: "src/pages/MealPlanner/MealPlanner.tsx"
      to: "src/lib/services/planner.ts"
      via: "saveMealPlan() call"
---

<objective>
Implement the Sunday-start weekly calendar UI and integrate the AI generation flow using Google Stitch and Vanilla CSS.

Purpose: Core user interface for viewing and creating meal plans.
Output: Weekly calendar page with generation sidebar.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-meal-planner-core/04-01-SUMMARY.md
@.planning/phases/04-meal-planner-core/04-RESEARCH.md
@src/lib/services/planner.ts
@src/components/Layout/Shell.tsx
</context>

<interfaces>
From src/lib/services/planner.ts:
```typescript
export interface Recipe {
  id: string;
  name: string;
  ingredients: any[];
  instructions: any[];
  nutrition: any;
}

export interface MealPlanSlot {
  day_of_week: number;
  meal_type: 'breakfast' | 'lunch' | 'dinner' | 'snack';
  recipe_id?: string;
  is_locked: boolean;
  manual_entry?: string;
}

export interface MealPlan {
  id: string;
  week_start_date: string;
  status: 'draft' | 'published';
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Weekly Calendar UI Components</name>
  <files>src/components/MealPlanner/PlannerGrid.tsx, src/components/MealPlanner/DayColumn.tsx, src/components/MealPlanner/MealSlot.tsx</files>
  <action>
    - Create `PlannerGrid`: A 7-column layout (Sunday-Saturday) using CSS Grid.
    - Create `DayColumn`: Displays day name and date, contains multiple `MealSlot` components.
    - Create `MealSlot`: Displays meal type (B/L/D) and the recipe name or manual entry.
    - Use **Vanilla CSS** for all styling, following the "earthy minimalist" theme.
    - Use `date-fns` (`startOfWeek`, `addDays`, `format`) to calculate dates for the current view.
    - Component generation: Use `stitch-design` for visual patterns.
  </action>
  <verify>
    <automated># Check if calendar components exist and export correctly</automated>
  </verify>
  <done>UI components for the 7-day grid are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Generation Sidebar & State</name>
  <files>src/components/MealPlanner/GenerationPanel.tsx, src/pages/MealPlanner/MealPlanner.tsx</files>
  <action>
    - Create `GenerationPanel`: A sidebar containing:
      - Week selector (Previous/Next week navigation).
      - Meal selection checkboxes (Breakfast, Lunch, Dinner, Snack).
      - "Cook once, eat twice" (Leftover strategy) toggle.
      - "Generate Plan" button.
    - Implement `MealPlanner` page:
      - Manage state for current week, selected meals, and current plan data.
      - Integrate `PlannerGrid` and `GenerationPanel`.
      - Wire "Generate Plan" to call AI Proxy (via `src/lib/ai/client.ts`) with a structured prompt.
  </action>
  <verify>
    <automated># Check if files exist and have required exports</automated>
  </verify>
  <done>Generation UI and navigation logic implemented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint - UI Visual Audit</name>
  <files>src/pages/MealPlanner/MealPlanner.tsx</files>
  <action>
    Verify the Sunday-start weekly calendar with generation sidebar matches the design requirements.
    - Confirm Sunday is the first day of the week.
    - Test Next/Previous week navigation.
    - Verify the generation sidebar is visible and matches the "earthy cookbook" aesthetic.
  </action>
  <verify>
    <automated># Manual verification required</automated>
  </verify>
  <done>UI layout and navigation approved by human.</done>
</task>

<task type="auto">
  <name>Task 4: Integrated AI Generation Flow</name>
  <files>src/pages/MealPlanner/MealPlanner.tsx, src/lib/ai/prompts.ts</files>
  <action>
    - Create `src/lib/ai/prompts.ts` containing the complex prompt engineering for the "Multi-Recipe Hybrid" strategy.
    - Prompt should inject: Household members, dietary profiles, selected meals, and leftover preferences.
    - Define JSON response schema in the prompt (matching the DB slots/recipes).
    - Update `MealPlanner` page to handle AI response:
      - Parse the `recipes` and `plan` from the JSON response.
      - Call `plannerService.saveMealPlan` to persist to Supabase.
      - Refresh the local state to show the new plan.
  </action>
  <verify>
    <automated># Verify AI prompts file exists and MealPlanner integrates with it</automated>
  </verify>
  <done>End-to-end generation and persistence flow is functional.</done>
</task>

</tasks>

<verification>
Unit tests for calendar UI pass. Manual verification of UI layout and navigation. AI integration tested with mock/real provider.
</verification>

<success_criteria>
- User can navigate week-to-week.
- UI displays a 7-day grid starting on Sunday.
- AI generation triggers a save to Supabase and updates the UI with recipes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-meal-planner-core/04-02-SUMMARY.md`
</output>
