---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/supabase.ts
  - supabase/migrations/20260411000000_initial_schema.sql
  - src/__tests__/db.test.ts
autonomous: true
requirements: [FOUNDATION-01, DB-01, AI-PROXY-01]
---

<objective>
Establish the technical foundation by scaffolding the React + Vite + TS project, configuring the Supabase database schema with RLS, and initializing the Supabase client.

Purpose: Provide a secure, type-safe foundation for the meal planner app.
Output: Working frontend scaffold and Supabase database with RLS.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Testing Scaffold</name>
  <files>package.json, vitest.config.ts, src/__tests__/setup.ts</files>
  <action>
    Initialize Vitest for testing.
    1. Install `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom` as devDependencies.
    2. Create `vitest.config.ts` and `src/__tests__/setup.ts`.
    3. Add `"test": "vitest run"` to scripts in `package.json`.
  </action>
  <verify>
    <automated>npm run test -- --version</automated>
  </verify>
  <done>Testing framework installed and configurable.</done>
</task>

<task type="auto">
  <name>Task 1: Project & Supabase Client Scaffold</name>
  <files>package.json, src/lib/supabase.ts, .env.example</files>
  <action>
    Scaffold the React + Vite + TS project and initialize the Supabase client.
    1. Run `npm create vite@latest . -- --template react-ts` (force in current directory if needed).
    2. Install core dependencies: `@supabase/supabase-js`, `react-router-dom`, `lucide-react`, `clsx`.
    3. **CRITICAL:** Ensure NO TailwindCSS is installed or configured (per GEMINI.md). Use Vanilla CSS.
    4. Create `src/lib/supabase.ts` using `createClient`.
    5. Create `.env.example` with `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`.
    6. Scaffold `supabase/functions/` directory for future Edge Functions (per Phase 1 goals).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Project scaffolded, Supabase client initialized, build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Database Schema & RLS Policies</name>
  <files>supabase/migrations/20260411000000_initial_schema.sql</files>
  <action>
    Define the Supabase database schema and RLS policies (D-07, D-08).
    1. Create `households` table: `id` (uuid, pk), `owner_id` (uuid, references auth.users), `created_at` (timestamptz).
    2. Create `household_members` table: `id` (uuid, pk), `household_id` (uuid, references households), `name` (text), `nutrition_profile` (jsonb), `created_at` (timestamptz).
    3. Enable RLS on both tables.
    4. Policy: `households` access only by `owner_id = auth.uid()`.
    5. Policy: `household_members` access only if their `household_id` belongs to the current user.
  </action>
  <verify>
    <automated>grep -i "CREATE TABLE" supabase/migrations/*.sql && grep -i "ENABLE ROW LEVEL SECURITY" supabase/migrations/*.sql</automated>
  </verify>
  <done>Database schema and security policies defined in migration file.</done>
</task>

</tasks>

<verification>
Check that the project compiles and the database migration contains the correct table and RLS definitions.
</verification>

<success_criteria>
- Project scaffolds successfully with TypeScript.
- Supabase client is initialized.
- Database migration file contains `households` and `household_members` with strict RLS.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
