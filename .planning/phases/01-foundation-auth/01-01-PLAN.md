---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/supabase.ts
  - supabase/migrations/20260411000000_initial_schema.sql
  - supabase/functions/ai-proxy/index.ts
  - src/__tests__/db.test.ts
  - .env.example
autonomous: true
requirements: [FOUNDATION-01, DB-01, AI-PROXY-01]
must_haves:
  truths:
    - "React + Vite + TS project compiles successfully."
    - "Supabase client is initialized and exported."
    - "Database schema for Households and Profiles is defined with RLS."
    - "AI Proxy edge function handles POST requests and validates JWT."
  artifacts:
    - path: "src/lib/supabase.ts"
      provides: "Supabase client instance"
    - path: "supabase/migrations/20260411000000_initial_schema.sql"
      provides: "Initial schema with RLS"
    - path: "supabase/functions/ai-proxy/index.ts"
      provides: "Functional AI proxy in Deno"
  key_links:
    - from: "supabase/migrations"
      to: "auth.users"
      via: "Foreign key reference"
    - from: "src/lib/supabase.ts"
      to: "import.meta.env"
      via: "Configuration"
---

<objective>
Establish the technical foundation by scaffolding the React + Vite + TS project, configuring the Supabase database schema with RLS, and implementing a functional AI Proxy Edge Function.

Purpose: Provide a secure, type-safe foundation with functional AI routing.
Output: Working frontend scaffold, Supabase database with RLS, and AI Proxy Edge Function.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@GEMINI.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Testing Scaffold</name>
  <files>package.json, vitest.config.ts, src/__tests__/setup.ts</files>
  <action>
    Initialize Vitest for testing.
    1. Install `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom` as devDependencies.
    2. Create `vitest.config.ts` and `src/__tests__/setup.ts`.
    3. Add `"test": "vitest run"` to scripts in `package.json`.
  </action>
  <verify>
    <automated>npm run test -- --version</automated>
  </verify>
  <done>Testing framework installed and configured.</done>
</task>

<task type="auto">
  <name>Task 1: Project & Supabase Client Scaffold</name>
  <files>package.json, src/lib/supabase.ts, .env.example</files>
  <action>
    Scaffold the React + Vite + TS project and initialize the Supabase client.
    1. Run `npm create vite@latest . -- --template react-ts` (force in current directory if needed).
    2. Install core dependencies: `@supabase/supabase-js`, `react-router-dom`, `lucide-react`, `clsx`.
    3. **CRITICAL: Ensure NO TailwindCSS is installed or configured. All styling must be Vanilla CSS (per GEMINI.md).**
    4. Create `src/lib/supabase.ts` using `createClient`.
    5. Create `.env.example` with `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`.
  </action>
  <verify>
    <automated>npm run build && ! grep -r "tailwind" .</automated>
  </verify>
  <done>Project scaffolded, Supabase client initialized, no Tailwind detected.</done>
</task>

<task type="auto">
  <name>Task 2: Database Schema & RLS Policies</name>
  <files>supabase/migrations/20260411000000_initial_schema.sql</files>
  <action>
    Define the Supabase database schema and RLS policies (D-07, D-08).
    1. Create `households` table: `id` (uuid, pk, default gen_random_uuid()), `owner_id` (uuid, references auth.users), `created_at` (timestamptz).
    2. Create `household_members` table: `id` (uuid, pk, default gen_random_uuid()), `household_id` (uuid, references households), `name` (text), `nutrition_profile` (jsonb), `created_at` (timestamptz).
    3. Enable RLS on both tables.
    4. Policy: `households` access only by `owner_id = auth.uid()`.
    5. Policy: `household_members` access only if their `household_id` belongs to the current user (using subquery check against `households`).
  </action>
  <verify>
    <automated>grep -i "CREATE TABLE" supabase/migrations/*.sql && grep -i "ENABLE ROW LEVEL SECURITY" supabase/migrations/*.sql</automated>
  </verify>
  <done>Database schema and security policies defined in migration file.</done>
</task>

<task type="auto">
  <name>Task 3: Functional AI Proxy Edge Function</name>
  <files>supabase/functions/ai-proxy/index.ts</files>
  <action>
    Implement a functional AI Proxy Edge Function in Deno.
    1. Create `supabase/functions/ai-proxy/index.ts`.
    2. The function must:
       - Handle POST requests.
       - Validate the user's JWT from the `Authorization` header using Supabase's `auth` helper.
       - Accept a payload like `{ provider: string, prompt: string }`.
       - For now, implement a "ping" mode that returns `{ status: "ok", message: "AI Proxy is functional" }`.
       - Implement logic to fetch from an AI provider (e.g., Gemini) if an environment variable (e.g., `GEMINI_API_KEY`) is present.
    3. Ensure it uses standard Supabase Edge Function response headers (CORS).
  </action>
  <verify>
    <automated>cat supabase/functions/ai-proxy/index.ts | grep "serve" && cat supabase/functions/ai-proxy/index.ts | grep "Authorization"</automated>
  </verify>
  <done>Functional AI proxy implemented in Supabase Edge Functions.</done>
</task>

</tasks>

<verification>
Check that the project compiles, no Tailwind is present, the database migration is correct, and the AI proxy contains JWT validation logic.
</verification>

<success_criteria>
- Project scaffolds successfully with TypeScript.
- Supabase client is initialized.
- Database migration file contains Households and Members with strict RLS.
- AI Proxy Edge Function is functional and secure (JWT validation).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
