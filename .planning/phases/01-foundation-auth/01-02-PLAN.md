---
phase: 01-foundation-auth
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .stitch/DESIGN.md
  - src/styles/theme.css
  - src/contexts/AuthContext.tsx
  - src/components/Auth/ProtectedRoute.tsx
  - src/__tests__/auth.test.tsx
autonomous: true
requirements: [DESIGN-01, AUTH-01]
must_haves:
  truths:
    - "Design tokens for Newsreader and Sage Green are defined in DESIGN.md."
    - "Vanilla CSS theme provides global earthy aesthetic (no Tailwind)."
    - "AuthContext provides session state and magic link trigger function."
    - "ProtectedRoute correctly redirects unauthenticated users."
  artifacts:
    - path: ".stitch/DESIGN.md"
      provides: "Stitch design tokens"
    - path: "src/styles/theme.css"
      provides: "Vanilla CSS variables and global styles"
    - path: "src/contexts/AuthContext.tsx"
      provides: "Session and auth logic"
  key_links:
    - from: "src/contexts/AuthContext.tsx"
      to: "supabase.auth.onAuthStateChange"
      via: "Subscription for session updates"
---

<objective>
Establish the design system foundation and core authentication logic using Google Stitch and Supabase.

Purpose: Provide the visual identity and secure session management.
Output: Design tokens, global CSS theme (Vanilla), and functional AuthContext.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@GEMINI.md

<interfaces>
From src/lib/supabase.ts:
```typescript
import { createClient } from '@supabase/supabase-js';
export const supabase = createClient(URL, KEY);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design System Foundation (Stitch)</name>
  <files>.stitch/DESIGN.md, src/styles/theme.css</files>
  <action>
    Define the design tokens and generate vanilla CSS variables (D-04, D-05, D-06, D-10).
    1. Create `.stitch/DESIGN.md` using Google Stitch syntax:
        - Theme: "minimalist earthy aesthetic", "cookbook layout"
        - Headline Font: Newsreader (Serif)
        - Primary Color: Sage Green (#4A6741)
        - Radius: 8px (Balanced)
    2. Create `src/styles/theme.css` using vanilla CSS variables.
    3. Include a `.centered-content` class with `max-width: 800px` and `margin: 0 auto`.
    4. Ensure the body font is clean and readable (Inter/Manrope).
    5. **STRICTLY NO TAILWIND.** Use plain CSS for all layout utilities (flex, grid, spacing).
  </action>
  <verify>
    <automated>grep -i "Newsreader" .stitch/DESIGN.md && grep -i "#4A6741" src/styles/theme.css && ! grep -r "tw-" src/styles/</automated>
  </verify>
  <done>Design system defined and CSS variables generated in Vanilla CSS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auth Context & Protected Routing</name>
  <files>src/contexts/AuthContext.tsx, src/components/Auth/ProtectedRoute.tsx, src/__tests__/auth.test.tsx</files>
  <behavior>
    - AuthProvider: Provides session state and `signInWithMagicLink(email)` function.
    - ProtectedRoute: Redirects to `/login` if session is null.
    - Session: Lasts 7 days (managed by Supabase default).
  </behavior>
  <action>
    Implement authentication logic (D-01, D-02).
    1. Create `AuthContext.tsx` managing `session` from `supabase.auth.onAuthStateChange`.
    2. Add `signInWithMagicLink(email)` using Supabase `signInWithOtp`.
    3. Create `ProtectedRoute.tsx` component wrapper for authenticated routes.
    4. Verify logic with Vitest in `auth.test.tsx`.
  </action>
  <verify>
    <automated>npm run test -- --filter=auth</automated>
  </verify>
  <done>Authentication context and protected route component implemented.</done>
</task>

</tasks>

<verification>
Verify that `DESIGN.md` reflects the locked decisions, the theme is Vanilla CSS, and the auth tests pass.
</verification>

<success_criteria>
- `.stitch/DESIGN.md` contains the Newsreader and Sage Green tokens.
- `AuthContext` provides session state and can trigger magic link emails.
- `ProtectedRoute` correctly gates content.
- No Tailwind classes are used.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-02-SUMMARY.md`
</output>
