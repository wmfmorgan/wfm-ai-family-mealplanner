---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/pages/Auth/Login.tsx
  - src/components/Layout/Shell.tsx
  - src/components/Layout/Sidebar.tsx
  - src/components/Layout/BottomTabBar.tsx
  - src/App.tsx
  - src/index.css
  - src/__tests__/layout.test.tsx
autonomous: false
requirements: [SHELL-01, AUTH-01]
must_haves:
  truths:
    - "User can enter email and trigger a magic link login."
    - "App Shell shows a Sidebar on desktop and Bottom Tab Bar on mobile."
    - "Main content area is constrained to 800px max width and centered."
    - "Typography (Newsreader) and Sage Green accent colors are visible."
  artifacts:
    - path: "src/pages/Auth/Login.tsx"
      provides: "Login UI"
    - path: "src/components/Layout/Shell.tsx"
      provides: "Global layout shell"
    - path: "src/components/Layout/Sidebar.tsx"
      provides: "Desktop navigation"
    - path: "src/components/Layout/BottomTabBar.tsx"
      provides: "Mobile navigation"
  key_links:
    - from: "src/pages/Auth/Login.tsx"
      to: "AuthContext"
      via: "signInWithMagicLink usage"
    - from: "src/App.tsx"
      to: "Shell.tsx"
      via: "Route wrapping"
---

<objective>
Implement the core authentication interface and the responsive application shell layout using the "cookbook" aesthetic and Vanilla CSS.

Purpose: Provide the entry point and the primary navigational interface.
Output: Magic Link login page and the Sidebar/Bottom Tab shell.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-02-SUMMARY.md
@GEMINI.md

<interfaces>
From src/contexts/AuthContext.tsx:
```typescript
export interface AuthContextType {
  user: User | null;
  session: Session | null;
  signInWithMagicLink: (email: string) => Promise<void>;
  signOut: () => Promise<void>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Magic Link Login UI</name>
  <files>src/pages/Auth/Login.tsx, src/__tests__/login.test.tsx</files>
  <action>
    Implement the login UI for magic link authentication (D-01).
    1. Create a clean form with an email input and a "Send Magic Link" button.
    2. Use **Vanilla CSS** for styling: earthy tones, Newsreader serif for the title.
    3. Wire the form to the `signInWithMagicLink` function from `AuthContext`.
    4. Display a "Check your email" success message or error message.
    5. Ensure post-login redirect leads to `/planner` (D-03).
    6. **STRICTLY NO TAILWIND.**
  </action>
  <verify>
    <automated>npm run test -- --filter=login</automated>
  </verify>
  <done>Login page created with Vanilla CSS and wired to magic link flow.</done>
</task>

<task type="auto">
  <name>Task 2: Responsive Cookbook Shell (Stitch + Vanilla CSS)</name>
  <files>src/components/Layout/Shell.tsx, src/components/Layout/Sidebar.tsx, src/components/Layout/BottomTabBar.tsx, src/App.tsx, src/index.css</files>
  <action>
    Build the global application shell layout (D-09, D-10).
    1. Create `Sidebar.tsx`: Visible on desktop, hidden on mobile (via media queries). Uses Sage Green for active states and Newsreader for branding.
    2. Create `BottomTabBar.tsx`: Visible on mobile, hidden on desktop.
    3. Create `Shell.tsx`: High-level wrapper that uses `.centered-content` (800px max-width) for the main page area.
    4. Update `src/index.css` with global base styles: earthy background, font-family setup.
    5. Configure `App.tsx` with `react-router-dom` routes:
        - `/login`: Public
        - `/planner`: Protected
        - `/household`: Protected
        - `/settings`: Protected
    6. **STRICTLY NO TAILWIND.**
  </action>
  <verify>
    <automated>grep -i "@media" src/components/Layout/Shell.tsx || grep -i "@media" src/index.css</automated>
  </verify>
  <done>Responsive shell and routing configured with Vanilla CSS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Phase 1 Foundation, Auth & Shell</what-built>
  <how-to-verify>
    1. Run `npm run dev`.
    2. Visit `/login`: Verify magic link form layout (Newsreader title, Sage Green accents).
    3. Test desktop view: Ensure sidebar is present and content is centered at 800px.
    4. Test mobile view (Inspect -> Mobile): Ensure sidebar vanishes and bottom tab bar appears.
    5. Confirm **NO** tailwind classes exist in the DOM (inspect elements).
    6. Verify that an unauthenticated attempt to visit `/planner` redirects to `/login`.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Manual visual audit and functional verification of responsive layout and auth flow.
</verification>

<success_criteria>
- Login page works with magic link.
- Layout is responsive (Sidebar on desktop / Tabs on mobile).
- Max-width 800px is enforced for content.
- Newsreader serif and Sage Green palette are correctly applied.
- Zero TailwindCSS usage.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
