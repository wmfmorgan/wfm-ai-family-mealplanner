---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/pages/Auth/Login.tsx
  - src/components/Layout/Shell.tsx
  - src/components/Layout/Sidebar.tsx
  - src/components/Layout/BottomTabBar.tsx
  - src/App.tsx
  - src/__tests__/layout.test.tsx
autonomous: false
requirements: [SHELL-01, AUTH-01]
---

<objective>
Implement the core authentication interface and the responsive application shell layout using the "cookbook" aesthetic.

Purpose: Provide the entry point and the primary navigational interface.
Output: Magic Link login page and the Sidebar/Bottom Tab shell.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-02-SUMMARY.md

<interfaces>
From src/contexts/AuthContext.tsx:
```typescript
export interface AuthContextType {
  user: User | null;
  session: Session | null;
  signIn: (email: string) => Promise<void>;
  signOut: () => Promise<void>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Magic Link Login Page</name>
  <files>src/pages/Auth/Login.tsx, src/__tests__/login.test.tsx</files>
  <action>
    Implement the login UI for magic link authentication (D-01).
    1. Create a simple form with an email input and a "Send Magic Link" button.
    2. Wire it to the `signInWithMagicLink` function from `AuthContext`.
    3. Show a success/error message feedback.
    4. Implement post-login redirect to the Weekly Planner (D-03).
    5. **D-07 Check**: Add logic on login success to ensure a Household record exists for the user.
  </action>
  <verify>
    <automated>npm run test -- --filter=login</automated>
  </verify>
  <done>Login page created and wired to magic link flow with household initialization.</done>
</task>

<task type="auto">
  <name>Task 2: Responsive Cookbook Shell</name>
  <files>src/components/Layout/Shell.tsx, src/components/Layout/Sidebar.tsx, src/components/Layout/BottomTabBar.tsx, src/App.tsx</files>
  <action>
    Build the global application shell layout (D-09, D-10).
    1. Create `Sidebar.tsx`: Visible on desktop, hidden on mobile. Newsreader serif for logo/headings.
    2. Create `BottomTabBar.tsx`: Visible on mobile, hidden on desktop.
    3. Create `Shell.tsx`: Wraps protected pages, enforces the 800px centered content width.
    4. Wire `App.tsx` with `react-router-dom`:
        - `/login`: Public
        - `/planner`: Protected landing page
        - `/household`: Protected
        - `/settings`: Protected
  </action>
  <verify>
    <automated>grep -i "Sidebar" src/components/Layout/Shell.tsx && grep -i "BottomTabBar" src/components/Layout/Shell.tsx</automated>
  </verify>
  <done>Global responsive shell and routing configured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Phase 1 Foundation & Auth</what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit the app.
    2. Verify the Login page: Enter email and check magic link trigger.
    3. Inspect layout on Desktop (>800px): Confirm Sidebar is visible and content is centered.
    4. Inspect layout on Mobile: Confirm Sidebar is hidden and Bottom Tab Bar is visible.
    5. Check typography: Verify Newsreader serif headings and Sage Green accents (#4A6741).
    6. Verify that unauthenticated access to /planner redirects to /login.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Manual visual audit and functional auth flow verification.
</verification>

<success_criteria>
- Login page works with magic link.
- Layout is responsive (Sidebar/Tabs).
- Max-width 800px is enforced.
- Newsreader and Sage Green aesthetic is applied.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
